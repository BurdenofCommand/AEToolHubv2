# TODO — Structure & Base Costs (v5.0)

Not yet populated. This folder is reserved for:

- Per-base construction costs (all bases, v5.0)
- Per-level structure costs (v5.0)

Per repo guidelines (`ReferenceData` structure doc), base costs and structure costs both live here rather than in a separate top-level "Bases" category.
